#!/bin/bash
# Run Claude CLI directly. The --dangerously-skip-permissions flag is blocked
# when running as root, so we use --permission-prompt-tool none instead.
# The -p flag (print mode) doesn't invoke tools, so permissions are moot.
exec /app/cli/claude "$@"
